.. option:: -h, --help

    show this help message and exit

.. option:: FILE

    PDF filename to process

.. option:: --command COMMAND, -c COMMAND

    run initial commands before showing prompt (split multiple commands with ';').

.. option:: --Command, -C

    run initial commands before showing prompt (from standard input).
